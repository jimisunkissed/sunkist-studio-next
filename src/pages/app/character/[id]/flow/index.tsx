import { ReactFlow } from '@/lib/component/react-flow/react-flow-page';
import React, { ReactNode } from 'react';

function CharacterDetailEdit(): ReactNode {
  return <ReactFlow />;
}

export default CharacterDetailEdit;
