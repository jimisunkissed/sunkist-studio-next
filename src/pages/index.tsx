import React from 'react';
import { FrontNavbar } from '@/lib/component/navigation/front-navbar';

function HomePage() {
  return (
    <div className="relative flex flex-col h-screen w-screen">
      <FrontNavbar />
    </div>
  );
}

export default HomePage;
